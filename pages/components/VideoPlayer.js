// Remove unused import
// import { createClient } from '../utils/supabase/component';

// Fix unused parameter
const handleError = (_e) => { // Prefix with underscore to indicate it's intentionally unused
  setHasError(true);
}; 